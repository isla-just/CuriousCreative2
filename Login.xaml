<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TheCuriousCreative.Login"
             Title="Login">

       <StackLayout>
        <Label Text="Hi This is Login!"
                VerticalOptions="Center" 
                HorizontalOptions="Center" />

        <Label
            BindingContext="{x:Reference SecondPage}"
            Text="{Binding helloText}"
            TextColor="Red"
            FontSize="Large"/>

         <Button Text="Navigate Back" Clicked="Back_Clicked"/>
    </StackLayout>
</ContentPage>

